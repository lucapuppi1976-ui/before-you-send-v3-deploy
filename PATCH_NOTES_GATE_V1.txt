BYS Gate v1 + checkpoint locale fix

What changed:
- early-access gate screen before onboarding/home
- unlock code checked server-side
- signed access cookie stored httpOnly
- protected decode/send/config endpoints
- settings action to lock the app again on this device
- gate locale keys added for IT / EN / ES
- service-worker cache bumped again to invalidate stale cached locales
- render.yaml now declares BYS_GATE_ENABLED explicitly

Render env to keep/add:
- BYS_ACCESS_CODE (private)
- BYS_GATE_SECRET (private)
- BYS_GATE_ENABLED = true
- optional BYS_ACCESS_PAGE_URL
